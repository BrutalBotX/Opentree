# See root project status in source workspace.

This exported GitHub-ready folder was produced from the active local workspace.

If you want a full live status dump in this folder too, copy the current `PROJECT_STATUS.md` from the working project root before publishing.
